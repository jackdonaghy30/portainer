# Portainer — exemplo de .env
# Copie para `.env` na mesma pasta do `docker-compose.yml` e ajuste os valores.
# Portainer não requer variáveis de ambiente obrigatórias para iniciar,
# mas este arquivo documenta as customizações disponíveis.

# Porta HTTP da interface web no host
PORTAINER_HTTP_PORT=9000

# Porta HTTPS da interface web no host
PORTAINER_HTTPS_PORT=9443
