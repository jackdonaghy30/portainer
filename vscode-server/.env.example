# code-server — exemplo de .env
# Copie para `.env` na mesma pasta do `docker-compose.yml` e ajuste os valores.

# Usuário e grupo do host
PUID=1000
PGID=1000

# Timezone
TZ=America/Sao_Paulo

# Senha de acesso ao code-server (obrigatória)
PASSWORD=replace-with-strong-password

# Senha sudo dentro do container (opcional; remova se não precisar)
SUDO_PASSWORD=

# Porta exposta no host
PORT=8444

# Diretório de configuração no host
CONFIG_LOCATION=./config

# Diretório do workspace no host (criado automaticamente pelo init-workspace)
HOST_WORKSPACE=./vscode/projects
