# Copy to .env and configure before use

# User and group ID of the host user (run `id` to check)
PUID=1000
PGID=1000

# Timezone
TZ=America/Sao_Paulo

# Password to access the code-server web UI
PASSWORD=change-me-strong-password

# Optional: sudo password inside the container (remove if not needed)
SUDO_PASSWORD=change-me-sudo-password

# Host port (access via http://<host>:8443)
PORT=8443

# Directory on the host to store config and workspace
CONFIG_LOCATION=./config

# Default workspace opened in the editor
DEFAULT_WORKSPACE=/config/workspace
