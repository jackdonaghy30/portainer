
# Speedtest Tracker - exemplo de .env
# Copie este arquivo para `.env` na mesma pasta do `docker-compose.yml` e ajuste.

# Usuário/Grupo para volumes (LinuxServer pattern)
PUID=1000
PGID=1000

# Timezone
TZ=Etc/UTC

# Aplicação
APP_KEY=
APP_URL=http://your-domain-or-ip
APP_ENV=production

# Banco de dados: sqlite (padrão), pgsql ou mysql
DB_CONNECTION=sqlite
# Se usar pgsql/mysql, configure as linhas abaixo:
#DB_HOST=
#DB_PORT=
#DB_DATABASE=
#DB_USERNAME=
#DB_PASSWORD=

# Agendamento do speedtest em formato cron (ex: a cada 6 horas)
SPEEDTEST_SCHEDULE=0 */6 * * *

# Lista de servidores (IDs) separados por vírgula — veja docs para `list-servers`
SPEEDTEST_SERVERS=

# Timezone exibida na UI (opcional)
DISPLAY_TIMEZONE=Etc/UTC

# Manter apenas resultados dos últimos N dias (0 para manter todos)
PRUNE_RESULTS_OLDER_THAN=0

