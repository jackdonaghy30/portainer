# Timezone do servidor
TZ=America/Sao_Paulo

# Porta de acesso à interface web do Netdata (padrão: 19999)
NETDATA_PORT=19999

# (Opcional) Token para vincular ao Netdata Cloud
# Obtenha em: https://app.netdata.cloud
NETDATA_CLAIM_TOKEN=

# (Opcional) URL do Netdata Cloud (padrão já configurado)
NETDATA_CLAIM_URL=https://app.netdata.cloud

# (Opcional) ID da sala no Netdata Cloud para associar este nó
NETDATA_CLAIM_ROOMS=
