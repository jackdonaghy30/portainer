# ============================================
# World Monitor — Variáveis de Ambiente (Build)
# ============================================
# Copie este arquivo para .env e ajuste os valores desejados.
#
#   cp .env.example .env
#
# ATENÇÃO: As variáveis VITE_* são incorporadas na hora do build.
# Alterar o .env após a imagem já construída não terá efeito —
# é necessário reconstruir a imagem com: docker compose build --no-cache
# ============================================

# Porta de acesso à interface web (padrão: 8083)
WORLDMONITOR_PORT=8083

# Variante do dashboard: full (padrão), tech ou finance
# - full    → worldmonitor.app (geopolítica, conflitos, infraestrutura)
# - tech    → tech.worldmonitor.app (startups, IA/ML, cibersegurança)
# - finance → finance.worldmonitor.app (mercados globais, bancos centrais)
VITE_VARIANT=full

# Modo de interação do mapa:
# - 3d  → habilita pitch/rotação (padrão)
# - flat → mantém o mapa em 2D
VITE_MAP_INTERACTION_MODE=3d

# (Opcional) DSN do Sentry para relatório de erros no cliente
VITE_SENTRY_DSN=

# (Opcional) PostHog para analytics de produto
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=
