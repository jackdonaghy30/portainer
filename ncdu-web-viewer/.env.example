## Exemplo de arquivo .env para este docker-compose
# Copie para `.env` e ajuste conforme necessário

# Modo de operação: "folder" (padrão) ou "dump"
# - folder: o ncdu escaneia a pasta /folder-to-scan/ dentro do container
# - dump: o ncdu importa o arquivo /ncdu-dump.json para visualização
NCDU_WEB_VIEWER_SCAN_FROM=folder

# Modo somente-leitura (apenas usado com SCAN_FROM=folder)
# "yes" (padrão) ou "no"
NCDU_WEB_VIEWER_READONLY=yes

# Caminho no host da pasta a ser analisada (usado com SCAN_FROM=folder)
FOLDER_TO_SCAN=/caminho/para/sua/pasta

# Caminho no host do arquivo de dump ncdu (usado com SCAN_FROM=dump)
NCDU_DUMP_FILE=/caminho/para/ncdu-dump.json

# Porta exposta no host
PORT=3000
