# Copy to .env and set secure values
# Example values — replace before use
ADMIN_TOKEN=replace-with-secure-token
# Example domain (change to your DuckDNS or other hostname)
DOMAIN="https://senhas.dagmarbezerra.duckdns.org"
# Allow user signups: true or false
SIGNUPS_ALLOWED=true
# Change HTTP port only if you also change the mapping in docker-compose.yml
#DATABASE_URL=postgres://user:password@postgres:5432/vaultwarden
