## Example .env for Glance (copy to .env and edit values)

# Timezone
TZ=Europe/Berlin

# Nginx proxy settings (if using an external proxy)
NGINX_PROXY_URL=
NGINX_EMAIL_ID=
NGINX_PASSWORD=

# Speedtest tracker integration (optional)
SPEEDTEST_URL=
SPEEDTEST_TRACKER_API_TOKEN=

# Notes:
# - Copy this file to .env in the same folder as docker-compose.yml
# - Values left empty should be filled before starting the container